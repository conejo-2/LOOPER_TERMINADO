{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Looper (HTTP + Browser)",
      "type": "chrome",
      "request": "launch",
  "url": "http://localhost:8000/LOOPER_MENU.html",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Run Simple HTTP Server",
      "runtimeArgs": [
        "--new-window",
        "--disable-extensions"
      ]
    }
  ]
}